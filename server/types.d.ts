// This is where you should be defining your type declarations

// This is a type declaration file, so we can't use import/export

// IE 
// interface NewType {
//   name: string;
//   age: number;
// }

// Then you can use these types anywhere in your application without importing or exporting