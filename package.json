{
  "name": "pern-todo",
  "version": "1.0.0",
  "description": "PERN stack To-Do App.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "if-env TYPE=server && (cd server && npm run build) || (cd client && npm run build)",
    "server": "node ./server/app.js",
    "client": "cd client && npm start"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Grant-Greer/pern-todo.git"
  },
  "author": "Grant",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Grant-Greer/pern-todo/issues"
  },
  "homepage": "https://github.com/Grant-Greer/pern-todo#readme",
  "dependencies": {
    "dotenv": "^16.0.3",
    "if-env": "^1.0.4"
  }
}
